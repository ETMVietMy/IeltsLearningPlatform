<tr class='clickable-row' data-href="<%= message_path(message) %>">
  <% if !message.is_read %>
    <td><strong> <%= sender %> </strong></td>
    <td><strong><%= message.subject %></strong></td>
    <td><strong><%= message.getSentAt %></strong></td>

  <% else %>
    <td><%= sender %></td>
    <td><%= message.subject %></td>
    <td><%= message.getSentAt %></td>
  <% end %>
  <td>
    <%= link_to '<i class="fa fa-arrow-circle-right" aria-hidden="true"></i> View'.html_safe,
              message_path(id: message.id) %>
    |
    <%= link_to '<i class="fa fa-times-circle text-danger" aria-hidden="true"></i> Delete'.html_safe,
              message_path(id: message.id),
              { data: { confirm: "Are you sure you want to delete this message?" },
              method: :delete, remote: true, class: "delete-button"} %>
  </td>
</tr>
