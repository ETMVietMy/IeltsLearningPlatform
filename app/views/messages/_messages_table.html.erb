<table class="table table-hover table-striped">
    <thead>
        <tr>
            <th>Sender</th>
            <th>Subject</th>
            <th>Sent at</th>
        </tr>
    </thead>
    <tbody>
      <% @messages.each do |message| %>
      <%= render "message_row", message: message, sender: message.getSender %>
      <% end %>
    </tbody>
</table>
