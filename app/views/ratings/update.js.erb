

$('.review-rating').raty({
  readOnly: true,
  score: <%= @comment.average_rating %>,
  path: '/assets'
});

$('.average-review-rating').raty({
  score: <%= @rating.score %>,
  path: '/assets',
  click: function(score, evt) {
    $.ajax({
      url: '/ratings/' + <%= @rating.id %>,
      type: 'PATCH',
      data: { score: score }
    }).done(function (data){});
  }
});